const config = {
  db_info: {
    host: 'localhost',
    user: 'relay10',
    password: '1234',
    port: 3306,
    database: 'boost',
    connectionLimit: 50,
    dateStrings: 'date',
    multipleStatements: true
  }
}

module.exports = config;